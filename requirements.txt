# PartFinder — Python dependencies
# Pinned to conservative minimum versions; upgrade as needed.

Flask>=3.0,<4.0
requests>=2.31,<3.0
python-dotenv>=1.0,<2.0

# Testing (optional, used by tests/)
pytest>=8.0,<9.0
